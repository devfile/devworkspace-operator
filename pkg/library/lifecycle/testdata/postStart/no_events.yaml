name: "Should do nothing when devfile does not specify events"

input:
  debugEnabled: false
  devfile: {}
  containers:
    - name: test-container
      image: my-test-image

output:
  containers:
    - name: test-container
      image: my-test-image
