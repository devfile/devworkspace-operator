name: "Gives useful error when memory limit is less than default request"

input:
  components:
    - name: testing-container
      container:
        image: testing-image
        memoryLimit: 32m

output:
  errRegexp: "container memory limit \\(32m\\) is below default memory request \\(.*\\) and no value is supplied"
